project:
  name: paf-nfce
  description: Biblioteca java para geração de PAF-NFCe
  longDescription: |
    Biblioteca java para geração de PAF-NFCe (Programa Aplicativo Fiscal - Nota Fiscal do Consumidor Eletrônica).
    Facilita a criação e manipulação de arquivos PAF-NFCe em aplicações Java.
    
    ⚠️ **Compatibilidade**: Esta biblioteca é compatível com Java 8+ para garantir máxima compatibilidade.
  website: https://github.com/Java-Brasil/paf-nfce
  authors:
    - Rodrigo Aguiar Vidal Cananea
  license: MIT
  inceptionYear: 2025
  stereotype: NONE
  java:
    version: 8
    multiProject: false

release:
  github:
    owner: Java-Brasil
    name: paf-nfce
    tagName: 'v{{projectVersion}}'
    releaseName: 'Release v{{projectVersion}}'
    overwrite: true
    draft: false
    prerelease: false
    changelog:
      formatted: ALWAYS
      preset: conventional-commits
      contributors:
        format: '- {{contributorName}}'
      labelers:
        - label: 'dependencies'
          title: 'regex:^(?:deps(?:\(.*\))?!?):\s.*'
        - label: 'feature'
          title: 'regex:^(?:feat(?:\(.*\))?!?):\s.*'
        - label: 'bug'
          title: 'regex:^(?:fix(?:\(.*\))?!?):\s.*'
      categories:
        - title: '🚀 Features'
          labels:
            - 'feature'
        - title: '🐛 Bug Fixes'
          labels:
            - 'bug'
        - title: '📦 Dependencies'
          labels:
            - 'dependencies'
      hide:
        categories:
          - 'merge'
        contributors:
          - 'GitHub'

deploy:
  maven:
    nexus2:
      maven-central:
        active: ALWAYS
        url: https://s01.oss.sonatype.org/service/local
        closeRepository: true
        releaseRepository: true
        stagingRepositories:
          - target/staging-deploy
